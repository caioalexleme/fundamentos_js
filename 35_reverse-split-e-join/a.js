
// vamos ver mais alguns métodos para tratamentos de arrays

// reverse, split e join

console.clear()

// como o próprio nome indica, o reverse faz a inversão da ordem de um array
// não contém qualquer argumento no método

let nomes = new Array('caio', 'tiago', 'pedro', 'simao', 'estevão')

console.table(nomes.reverse())

// este método altera o próprio array