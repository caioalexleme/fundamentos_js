
// como fazer uma iteração num array?

let nomes = ['caio', 'ana', 'vitoria', 'joao', 'beto', 'camila']

// com o ciclo foreach como método do array, podemos fazer uma iteração e executar uma função para cada um dos elementos da coleção

console.clear()

nomes.forEach(function (e) {
    console.log(e)
})