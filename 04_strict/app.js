"use strict";
// Indica que existe erro no código, apesar de funcionar se tirar essa instrução, mas houve atualizações de sixtaxe, nesse caso existe um erro na definição da variável, precisa ter antes o tipo (const, var, let). Usado para forçar a linguagem a ser codificada na forma mais moderna.

nome = "caio";
console.log(nome);